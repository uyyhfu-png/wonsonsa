# 손해사정 사무소 홈페이지 + 고객관리 PRD (Product Requirements Document)

## 0. 사이트 구조 (2026-09-11 업데이트)
- 기존 "고객관리프로그램"을 **손해사정/보험상담 사무소 홍보용 홈페이지**로 전환
- **`/` (홈페이지)**: 일반 방문자용 마케팅 페이지 — domu.co.kr 참고. 히어로, 핵심 특징, 서비스 소개, 진행 절차, 상담신청 폼, 푸터로 구성
- **`/app/` (관리자, 구 고객관리프로그램)**: 기존 고객관리 기능을 그대로 유지 — 고객 목록(이름별 탭), 보험회사별 보기, 증권/진단서 등록, 그리고 홈페이지에서 들어온 **상담문의** 확인/고객 전환
- 홈페이지 상담신청 폼 제출 → `inquiries` 테이블에 저장 → `/app/`의 "상담문의" 탭에서 확인, "고객 추가로 전환" 버튼으로 바로 고객등록 폼에 이름/연락처 자동 반영

## 1. 개요
- **제품명**: OO손해사정 사무소 홈페이지 (회사명/연락처는 추후 실제 정보로 교체 필요)
- **목적**: 일반 방문자에게 손해사정/보험상담 서비스를 소개하고 상담 신청을 받으며, 내부적으로는 접수된 고객을 보험회사·진단명 기준으로 관리
- **주요 사용자**: (홈페이지) 보험금 청구를 고민하는 일반 방문자 / (관리자) 손해사정사·상담 담당자 본인
- **플랫폼**: 웹 앱 (로컬에서 실행, 브라우저로 접속)
- **기술 스택**: Node.js + Express (서버), SQLite(better-sqlite3, 로컬 DB 파일), 순수 HTML/CSS/JS (프론트엔드), Tesseract.js(OCR) + pdf-parse (증권/진단서 자동 인식)
- **데이터/파일 저장 위치**: 프로젝트 폴더 내 `data/` (DB 파일 `data/customers.db`, 첨부파일 `data/uploads/`) — 모두 로컬 폴더에 저장

## 2. 배경 및 문제 정의
- 고객이 여러 보험회사의 보험에 가입되어 있고, 진단명에 따라 청구·상담이 필요한 경우가 많음
- 현재는 고객 정보가 흩어져 있어 보험회사별, 진단명별로 빠르게 조회하기 어려움
- 신규 고객 등록 시 필요한 정보를 표준화하고, 보험회사 기준으로 분류·검색할 수 있는 도구가 필요함

## 3. 목표 (Goals)
- 고객 정보를 빠르고 정확하게 등록할 수 있다
- 가입한 보험회사별로 고객을 구분하여 조회할 수 있다
- 진단명 기준으로 고객을 검색·분류할 수 있다
- 고객 정보를 수정/삭제할 수 있다

### Out of Scope (1차 범위 제외)
- 보험금 청구 처리, 결제/정산 기능
- 다수 사용자 권한 관리(로그인/권한 분리) — 필요 시 추후 확장

## 4. 핵심 기능 (Features)

### 4.1 고객 등록
"고객관리에 파일(레코드)을 추가"하면 아래 항목을 입력하여 신규 고객을 등록한다.

| 필드명 | 설명 | 필수여부 | 비고 |
|---|---|---|---|
| 고객이름 | 고객 성명 | 필수 | 텍스트 |
| 소개자 | 고객을 소개해준 사람 | 선택 | 텍스트 |
| 주소 | 고객 거주지 주소 | 필수 | 텍스트 (도로명주소 권장) |
| 연락처 | 휴대폰/전화번호 | 필수 | 텍스트 (예: 010-000-0000) |
| 생년월일 | 고객 생년월일 | 필수 | 날짜 (YYYY-MM-DD) |
| 진단명 | 고객의 진단명 | 선택 | 복수 진단명 입력 가능(콤마 구분 등) |
| 추산금액 | 청구하려는 보험담보의 추산금액 | 선택 | 숫자(원 단위) |
| 가입 보험회사 | 고객이 가입한 보험회사명 | 필수 | 드롭다운(회사 목록) 또는 직접입력, 한 고객이 여러 보험회사에 가입한 경우 다중 선택 가능 |

- 한 명의 고객이 여러 보험회사에 가입되어 있을 수 있으므로, **고객 1건당 보험회사 다중 등록**을 지원해야 함
  - 예: 홍길동 - 삼성생명, 교보생명 각각 가입

### 4.1.1 증권/진단서 파일 등록
- 고객 등록 시(또는 등록 후) **보험 증권, 진단서 파일**을 첨부(업로드)할 수 있어야 함
- 파일 형식: PDF, 이미지(JPG/PNG) 등 스캔/촬영 파일 지원
- 파일 업로드 시 아래 정보를 함께 입력하여 고객과 매칭한다
  - 고객이름
  - 전화번호(연락처)
  - 주소
  - 생년월일
- 위 4가지 정보로 기존 고객과 일치하는지 확인(매칭)하고, 일치하는 고객이 있으면 해당 고객에 파일을 연결, 없으면 신규 고객 등록 여부를 확인
- 하나의 고객에게 **여러 건의 증권/진단서 파일**을 등록할 수 있음 (예: 증권 2건 + 진단서 1건)
- 파일 종류 구분: `증권` / `진단서` 선택(태그) 필요

### 4.2 고객 목록 조회
- 전체 고객 목록을 테이블 형태로 조회
- 목록에 표시할 기본 컬럼: 고객이름, 연락처, 가입 보험회사, 진단명

### 4.3 보험회사별 구분/필터
- 보험회사를 기준으로 고객을 그룹핑하여 볼 수 있는 화면 제공 (예: 좌측 보험회사 목록 클릭 시 해당 보험회사 가입 고객만 필터링)
- 보험회사 목록은 프로그램 내에서 관리(추가/수정) 가능해야 함

### 4.4 검색
- 고객이름, 연락처, 진단명, 보험회사명, 생년월일로 검색 가능

### 4.5 고객 정보 수정/삭제
- 등록된 고객 정보를 수정하거나 삭제할 수 있음
- 삭제 시 확인(컨펌) 절차 필요
- 고객 삭제 시 연결된 증권/진단서 파일도 함께 삭제할지 확인(컨펌) 필요

### 4.6 첨부 파일 조회
- 고객 상세 화면에서 해당 고객에게 등록된 증권/진단서 파일 목록을 조회
- 파일 목록에는 파일 종류(증권/진단서), 등록일, 파일명이 표시되고 클릭 시 열람/다운로드 가능
- 개별 파일 삭제 가능

## 5. 데이터 모델 (초안)

**Customer (고객)**
- id (고유 식별자)
- name (고객이름)
- referrer (소개자)
- address (주소)
- phone (연락처)
- birth_date (생년월일)
- diagnosis (진단명) — 1:N 가능성 고려
- estimated_amount (추산금액, 청구 예정 보험담보 추산금액)
- created_at / updated_at

**InsuranceCompany (보험회사)**
- id
- name (보험회사명)

**CustomerInsurance (고객-보험회사 매핑, N:M)**
- customer_id
- insurance_company_id
- (선택) 가입일, 상품명 등 추후 확장 필드

**CustomerDocument (고객 첨부파일 - 증권/진단서)**
- id
- customer_id (연결된 고객)
- doc_type (`증권` | `진단서`)
- file_path / file_name
- uploaded_at
- (매칭 시 사용) name, phone, address, birth_date — 업로드 당시 입력값, 고객 식별용

> 고객:보험회사 관계는 N:M 구조로, 한 고객이 여러 보험회사에 가입할 수 있고, 한 보험회사에는 여러 고객이 가입할 수 있음.
> 고객:첨부파일 관계는 1:N 구조로, 한 고객에게 여러 건의 증권/진단서 파일을 등록할 수 있음.

## 6. 화면 구성 (초안)
1. **고객 목록 화면**: 전체 고객 리스트 + 검색창 + "고객 추가" 버튼
2. **고객 추가/수정 화면(폼)**: 이름, 주소, 연락처, 생년월일, 진단명, 가입 보험회사(다중 선택) 입력
3. **보험회사별 보기**: 보험회사 목록 → 선택 시 해당 고객 리스트 표시
4. **파일 등록 화면**: 증권/진단서 파일 업로드 + 이름/전화번호/주소/생년월일 입력 → 기존 고객 매칭 또는 신규 등록
5. **고객 상세 화면**: 고객 정보 + 첨부된 증권/진단서 파일 목록

## 7. 비기능 요구사항
- 고객의 진단명, 생년월일, 증권·진단서 파일 등은 민감정보(개인 건강정보)이므로 데이터 접근/저장 시 보안 고려 필요 (추후 암호화, 접근 제어 등 확장 가능)
- 첨부 파일은 별도 저장 경로(폴더 또는 스토리지)에 저장하고, DB에는 경로/메타데이터만 저장
- 로컬 환경에서 우선 동작(향후 서버/DB 확장 고려)

## 8. 향후 확장 아이디어 (2차 이후)
- 보험 상품명, 가입일, 만기일 등 세부 정보 추가
- 진단명 기준 통계(보험회사별 진단 분포 등) 대시보드
- 고객 데이터 엑셀 가져오기/내보내기
- 다중 사용자 로그인 및 권한 관리
- 증권/진단서 파일에서 텍스트 자동 인식(OCR)하여 정보 자동 채움

## 9. 확정된 사항
- **플랫폼**: 웹 앱, 로컬에서 서버를 실행하고 브라우저로 접속
- **파일/데이터 저장 위치**: 로컬 폴더 (`data/customers.db`, `data/uploads/`)
- **진단명 입력 방식**: 자유 텍스트(콤마로 여러 개 구분)
- **보험회사 목록**: 고정 목록 없이 사용자가 자유롭게 입력하면 자동으로 목록에 추가됨
- **파일 매칭 규칙**: 이름/전화번호/주소/생년월일 4가지가 기존 고객 정보와 **모두 일치**해야 매칭됨(일치하는 고객이 없으면 등록 실패 안내)
- **파일 제한**: PDF, JPG, PNG만 허용 / 파일당 최대 20MB, 개수 제한 없음

## 10. 실행 방법
1. 프로젝트 폴더에서 의존성 설치(최초 1회): `npm install`
2. 서버 실행: `npm start`
3. 브라우저에서 `http://localhost:4300` 접속
